mkdir命令是创建目录(文件夹)的命令
语法：mkdir -p Linux路径
· 参数必填，表示Linux路径，即要创建的文件夹的路径，相对路径和绝对路径均可
· -p选项可选，表示自动创建不存在的父目录，适用于创建连续多层级的目录

注：快捷键 ctr+l相当于clear命令，作用是清空屏幕
       创建文件夹需要修改权限，请确保操作均在HOME目录内，设计到权限问题，HOME目录外无法成功