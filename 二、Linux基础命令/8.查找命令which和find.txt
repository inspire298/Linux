1. which命令
Linux中的命令本质是二进制可执行程序，类似于Windows系统中的.exe文件
我们可以通过which命令，查看所使用的一系列命令的程序文件的存放位置
语法：which 要查找的可执行程序或命令

2. find命令 -按文件名查找
在Linux中，我们可以通过find命令去搜索指定的文件
语法：find 起始路径 -name "被查找的文件名"

3. find命令 -按文件大小查找文件
语法：find 起始路径 -size +|-n[kMG]
· +、-表示大于和小于
· n表示大小数字
· KMG表示大小单位，k(小写字母)表示Kb，M表示MB，G表示GB